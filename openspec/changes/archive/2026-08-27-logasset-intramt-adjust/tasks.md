## 1. 参数定义与引用分析

- [x] 1.1 搜索 `PARAMID_LOGASSET_MATCHAMT_CONTAIN_INTRAMT`、`PARAMID_LOGASSET_BONDINTR`、`60207`、`25070401`、`BUSITYPE_MATCHAMT_INC_INTRAMT_UNSET` 的全部引用。
- [x] 1.2 确认旧控制项的每个运行时引用都已移除，或确认其保留用途不是资金股份流水利息处理生效逻辑。
- [x] 1.3 在合适的参数定义头文件中新增 `60209`、`60210`、`60211` 的含义明确的常量。

## 2. 资金股份流水运行时逻辑

- [x] 2.1 重构 `CLogassetCreator_::SpecialDealIntramt()` 或替代辅助方法，读取 `60209` 并执行 `matchamt += intramt; intramt = 0`。
- [x] 2.2 在 `InitLogasset()` 中新增 `60210` 处理，执行 `matchamt += intramt` 且保留 `intramt`。
- [x] 2.3 在 `InitLogasset()` 中新增 `60211` 处理，执行清零 `intramt` 且不改变 `matchamt`。
- [x] 2.4 保留 `013909/023909/503909` 独立逻辑，继续执行 `matchamt = abs(intrtax)` 并清零 `intramt`。
- [x] 2.5 为三个配置参数实现按逗号分隔 token 的业务类型精确匹配。
- [x] 2.6 移除资金股份流水利息处理逻辑中对旧参数 `60207` 和 `25070401` 的运行时读取。

## 3. 宏定义与共享引用清理

- [x] 3.1 将 `BUSITYPE_MATCHAMT_INC_INTRAMT_UNSET` 的运行时生效逻辑替换为 `60210` 配置。
- [x] 3.2 如果不存在有效的非运行时引用，则删除 `BUSITYPE_MATCHAMT_INC_INTRAMT_UNSET`；如果保留，需说明剩余引用不是生效控制来源。
- [x] 3.3 重新执行代码搜索，确认没有残留的旧参数和旧宏生效引用。

## 4. 标准版增量 Gauss 脚本

- [x] 4.0 仅使用 `last_version` 的 `full/gauss` 标准版脚本作为参数值推导依据，不修改本仓库 `full` 全量脚本。
- [x] 4.1 更新标准版 `patch/gauss/fs_cbs/fs_cbs_comm/2.data/sys_param_define.sql`，定义 `60209`、`60210`、`60211`，允许空字符串并设置足够长度。
- [x] 4.2 从标准版增量 Gauss 脚本中移除旧参数 `60207` 和 `25070401` 的定义。
- [x] 4.3 将标准版 `60209` 默认值设置为固定的“利息并入成交金额并清零利息”业务类型集合。
- [x] 4.4 将标准版 `60210` 默认值设置为原 `BUSITYPE_MATCHAMT_INC_INTRAMT_UNSET` 业务类型集合。
- [x] 4.5 将标准版 `60211` 默认值设置为空字符串。

## 5. 客户增量 Gauss 脚本

- [x] 5.0 仅使用 `last_version` 的 9 个客户 `full/<客户>/gauss` 脚本作为客户差异推导依据，不修改本仓库客户 `full` 全量脚本。
- [x] 5.1 更新东方财富增量 Gauss 脚本，使最终 `60209` 等于固定集合追加 `010201,010202,0202P1,0202P2`，`60210` 与标准版一致，`60211` 为空。
- [x] 5.2 更新国投证券增量 Gauss 脚本，使最终 `60209` 等于固定集合追加 `010201,010202,0202P1,0202P2`，`60210` 与标准版一致，`60211` 为空。
- [x] 5.3 更新广发证券增量 Gauss 脚本，使最终 `60209` 为空，`60210` 与标准版一致，`60211` 为空。
- [x] 5.4 更新国信证券增量 Gauss 脚本，使最终 `60209` 为空，`60210` 与标准版一致，`60211` 为 `015501,015503,025501,025503,024901,014901,014903,024903,025301,025304`。
- [x] 5.5 更新银河证券增量 Gauss 脚本，使最终 `60209` 为空，`60210` 与标准版一致，`60211` 为空。
- [x] 5.6 更新中信建投增量 Gauss 脚本，使最终 `60209` 为空，`60210` 与标准版一致，`60211` 为空。
- [x] 5.7 确认华兴证券、金证股份、中金财富继承标准版 `60209`、`60210`、空 `60211` 的行为；如果增量脚本规范要求显式覆盖，则新增对应覆盖。
- [x] 5.8 移除客户脚本中旧参数 `60207` 和 `25070401` 的覆盖。
- [x] 5.9 检查新增或修改的 `patch` SQL 路径是否符合数据库类型、清算场景、库名、脚本类别约定，并确认相关安装入口脚本会执行这些增量 SQL。

## 6. 验证

- [x] 6.1 按基线全量已执行、标准版增量先执行、客户增量后执行的顺序，验证所有客户最终 `60209`、`60210`、`60211` 值符合规格。
- [x] 6.2 验证 `013909`、`023909`、`503909` 不包含在 `60211` 中，且仍使用扣税业务独立覆盖逻辑。
- [x] 6.3 验证配置业务类型按完整 token 精确匹配，不发生子串误匹配。
- [x] 6.4 对已删除参数和宏引用执行定向代码搜索或静态检查。
- [x] 6.5 如果用户明确要求，执行相关构建目标或 pre-commit 检查；否则在交付说明中明确未执行构建。
